[project]
name = "llm-abyss-backend"
version = "0.1.0"
description = "ローカル LLM の Mechanistic Interpretability ツール"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
